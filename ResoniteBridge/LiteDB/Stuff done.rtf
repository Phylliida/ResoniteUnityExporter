modified Target frameworks to be target framework and set to .netstandard 2.1 and deleted other unneded projects

removed unneded attributes that were confusing it from csproj

updated system.buffers

actually just made a new csproj file

and fixed the few erros that showed up to make it compatible